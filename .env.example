# Servidor HTTP
NODE_ENV=development
HOST=127.0.0.1
PORT=3000

# MySQL. Preencha para persistir; vazio mantém o fallback temporário em memória.
# Formato: mysql://usuario:senha@host:3306/shoot_n_smash
DATABASE_URL=
