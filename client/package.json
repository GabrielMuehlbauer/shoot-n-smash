{
  "name": "@shoot-n-smash/client",
  "version": "0.9.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --configLoader native --host 0.0.0.0",
    "build": "vite build --configLoader native",
    "preview": "vite preview --configLoader native --host 0.0.0.0",
    "test": "node --test --test-isolation=none"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  }
}
